/** Build-time compatibility exports backed by the package runtime catalog. */
export {
  EVENT_SOUND_MAPPING,
  AUDIO_THEMES as THEMES,
  type AudioEvent as LogicalEvent,
  type AudioTheme as Theme,
} from "../../src/audio-catalog.js";
